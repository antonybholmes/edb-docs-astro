---
import DocIndex from '@/components/help/DocIndex.astro'
import { buildNodeTree, DOCS_DIR } from '@/lib/docs'

const [rootNode, _] = await buildNodeTree(DOCS_DIR)
---

<DocIndex rootNode={rootNode} node={rootNode} slug={[]} />
